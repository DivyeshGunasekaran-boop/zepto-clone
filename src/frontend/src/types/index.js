// Type definitions (JS placeholder — types are enforced at runtime via prop usage)
// Product: { id, name, brand, category, subcategory, imageUrl, price, originalPrice, discount, discountLabel, weight, rating, reviewCount, tags, inStock }
// CartItem: { product, quantity }
// CartState: { items, isOpen }
// Category: { id, label, icon }
